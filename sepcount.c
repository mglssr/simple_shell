#include "main.h"

/**
* sepcount - function that counts spaces between a string of arguments
* @buffer: stringof arguments
* @characters: number of characters read, including the delimiter char, ex null
* Return: the number of spaces between arguments, plus 2
*/

int sepcount(char *buffer, size_t buffsize)
{
	int rec = 0;

	while (buffer != NULL)
	{
		for(rec = 0; rec <= buffsize; rec++)
		{
			if (buffer[0] = " ")
				buff
			if (buffer[rec] = )
				{

				}
		}
	}
}
